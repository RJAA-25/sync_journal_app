<h1>Add Category</h1>

<%= form_with model: @category, url: create_category_path(@url_hash) do |f|%>

  <div>
    <%= f.label :title, "Title" %>
    <%= f.text_field :title %>
    <% @category.errors.full_messages_for(:title).each do |message| %>
			<p><%= message %></p>
		<% end %>
  </div>

  <div>
    <%= f.label :description, "Description" %>
    <%= f.text_field :description %>
  </div>

  <div>
    <%= f.submit "Add" %>
  </div>

<% end %>

<hr/>
<%#= button_to "Cancel", index_user_path(@url_hash), method: :get %>
<%= link_to "Cancel", index_user_path(@url_hash) %>